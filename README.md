# Laravel Livewire Base
```shell
git clone --depth=1 --branch=main https://github.com/jonaaix/laravel-livewire-base <yourname>
    
cd <yourname>

# Initialize Claude-Auto Repo
rm -rf ./laravel-livewire-base/.git \
&& git init \
&& git add . \
&& git commit -m "Initial commit" \
&& git switch -c dev \
&& git switch -c dev_ai
```
